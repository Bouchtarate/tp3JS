"use strict";

const product = { Nom, prix, description };

let pannier = [product, product, product];

const payment = { carte, num, date, CCV };

const CheckoutForm = {
  pannier: pannier,
  nom,
  prenom,
  userName,
  Adresse1,
  Adresse2,
  Ville,
  Pays,
  payment: payment,
};
